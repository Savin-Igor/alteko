# ── Local dev ──────────────────────────────────────────────────────────────────
# Copy this file to .env.local and fill in values.
# For production: copy to .env.production on the VPS (never commit).

# Database
# Local dev:  postgresql://postgres:postgres@localhost:5432/alteko
# Production: postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/alteko
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/alteko"

# PostgreSQL credentials (used by docker-compose.yml in production)
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="change-me-strong-password-in-production"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me-in-production-min-32-chars"

# Email (magic link)
EMAIL_SERVER_HOST="smtp.example.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="user@example.com"
EMAIL_SERVER_PASSWORD="password"
EMAIL_FROM="noreply@alteko.lv"

# OpenAI
OPENAI_API_KEY="sk-..."

# S3 / Object Storage
S3_ENDPOINT="https://s3.amazonaws.com"
S3_REGION="eu-central-1"
S3_BUCKET="alteko-files"
S3_ACCESS_KEY_ID="..."
S3_SECRET_ACCESS_KEY="..."

# External APIs
JANA_SETA_API_URL="https://api.kartes.lv"
JANA_SETA_API_KEY="..."
LVM_GEOSERVER_URL="https://lvmgeo.lvm.lv/geoserver/wfs"

# Data sync
VZD_DATA_URL="https://data.gov.lv/dati/lv/dataset/..."
BVKB_DATA_URL="https://data.gov.lv/dati/lv/dataset/..."

# Optional
SKIP_ENV_VALIDATION="false"
